# Claude Code Instructions

## Git Commit & Push

Quando l'utente chiede di committare e pushare, SEMPRE:

1. `git add .` (aggiungere TUTTI i file del progetto)
2. `git commit -m "messaggio"`
3. `git push`

NON selezionare file specifici. Usare SEMPRE `git add .` per includere tutto.
